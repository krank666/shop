const getters = {
    token: (state) => state.app.token,
    isCollapse: (state) => state.app.isCollapse,
    userInfo: (state) => state.app.userInfo,
    osType: (state) => state.app.osType
}
export default getters
